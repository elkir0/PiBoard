fastapi==0.115.0
uvicorn[standard]==0.30.0
websockets==13.0
python-dotenv==1.0.0

# Audio / Wake Word
# livekit-wakeword : detecteur principal (conv-attention, ~60x moins de faux
# declenchements qu'openWakeWord, CPU/offline). Modele entraine: audio/models/terminator_livekit.onnx
livekit-wakeword>=0.2.1
openwakeword>=0.6.0   # secours automatique si le modele livekit est absent
pyaudio==0.2.14
numpy

# Voice AI — Mistral (Voxtral STT + LLM ministral/mistral-small + Voxtral TTS via REST)
# Pin 1.12.x : la 2.x deplace Mistral sous le namespace mistralai.client (breaking).
mistralai==1.12.4

# TTS local de secours (voix FR offline, gratuite)
piper-tts>=1.4.0

# Musique
spotipy>=2.24.0
# Deezer (V3 — source musicale, ARL-only, valide sur Py3.11)
deezer-py>=1.3.7
pycryptodome>=3.23.0
httpx>=0.27.0

# Video
yt-dlp>=2024.11.18

# Meteo
requests>=2.32.0
